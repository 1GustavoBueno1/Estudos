services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: estudos
      POSTGRES_PASSWORD: estudos
      POSTGRES_DB: estudos
    volumes:
      - postgres_data:/var/lib/postgresql/
    ports:
      - "5432:5432"
volumes:
  postgres_data: